apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Chart.Name }}
  namespace: {{ .Values.namespace}}    
type: Opaque
data:
  JWT_SECRET: {{ .Values.jwtSecret | b64enc }}
